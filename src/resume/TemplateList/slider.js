/**
 * 
 */

class Swiper {
	constructor(obj) {
		super(obj)
		this.imgArr = obj.imgArr || [];
		this.scale = obj.scale || 0.8;
		this.gap = obj.gap;

		this.containerWidth = 600;
		this.imgWidth = obj.imgWidth;
		// this.
	}
}